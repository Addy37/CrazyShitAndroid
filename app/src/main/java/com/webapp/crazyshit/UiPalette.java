package com.webapp.crazyshit;

import android.graphics.Color;

/** Shared visual palette for the 2.8.1 yellow-on-OLED refresh. */
final class UiPalette {
    static final int PRIMARY = Color.rgb(251, 245, 6);
    static final int PRIMARY_DIM = Color.rgb(221, 216, 5);
    static final int PRIMARY_CONTAINER = Color.rgb(43, 42, 4);
    static final int ON_PRIMARY = Color.rgb(10, 10, 0);

    private UiPalette() {
    }
}
